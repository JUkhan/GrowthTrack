# Deferred Work

## Deferred from: code review of 1-0-project-scaffolding-deployment-foundation (2026-07-14)

- **Backup writes to a local Docker volume, not off-host storage (AC5)** [docker/docker-compose.yml:92-115, docker/backup/pg_dump_daily.sh] — Reason: blocked on hosting provider decision. Off-host storage can't be concretely implemented until a hosting provider is chosen, which is itself an explicitly deferred decision elsewhere in this story. Revisit once hosting is selected.
- **GET /health reports liveness only, not DB readiness** [api/routes/health.py] — Satisfies AC4's literal "liveness endpoint" wording, but since this endpoint drives every container's `restart: always` healthcheck, a fully-down database currently wouldn't trip automatic recovery. Only matters once a real DB-backed route exists (Story 1.1+).
- **Nginx only proxies /health and /webhooks/ to the API** [docker/nginx/nginx.conf] — Any future backend route (e.g. Story 1.1's login endpoint) needs a new `location` block added by hand, or it silently falls through to the SPA's `index.html`. Not a bug today (only these two routes exist), but a sharp edge the next story should know about.
- **create_engine()/create_session_factory() have no singleton caching** [adapters/persistence/database.py:22-24] — Calling `create_engine()` more than once would create redundant connection pools. Currently unreachable (nothing calls it yet); whoever wires up the first real repository should cache it as a singleton.
- **Postgres bound to 127.0.0.1:5432 on the host in all three environments** [docker/docker-compose.yml:27-28] — Via the single shared compose file. If a staging/production host ever runs another Postgres instance on 5432, this stack fails to start. Contingent on the still-undecided hosting/infra layout.
- **init-roles.sh only runs once via docker-entrypoint-initdb.d** [docker/postgres/init-roles.sh] — A disaster-recovery restore onto a fresh volume from a `pg_dump` (schema/data only, no roles) needs this script re-run manually. Standard Postgres behavior, undocumented; matters once a DR runbook exists.

## Deferred from: code review of 1-2-first-run-administrator-bootstrap (2026-07-17)

- **Bootstrap endpoint has no defense against a hostile first caller — permanent, unauthenticated admin-creation race** [api/auth/routes.py:130-156, domain/bootstrap.py, adapters/persistence/users.py:76-80] — Reason: `POST /auth/bootstrap` defends against a racing second caller and later callers, but not an anonymous visitor reaching a fresh instance before the real operator; also the advisory lock has no timeout (connection-pool exhaustion risk under a flood of unauthenticated requests). Decision: mitigation is expected to come from deployment/network isolation (e.g. firewalling until first Administrator creation), not app code. Revisit if network-level isolation isn't feasible for a given deployment.
- **Username collision with a pre-existing non-Administrator user is unhandled (bare 500, not the app's error envelope)** [domain/bootstrap.py] — Reason: currently unreachable — the only way to create any `users` row today is via this same bootstrap endpoint (which locks itself after the first Administrator); Epic 3 ("Manage Users"), the only future path that could seed a colliding non-Administrator username, is still backlog. Revisit when Epic 3 ships.
- **`BootstrapForm.tsx` duplicates `LoginPage.tsx`'s layout and submit-handling logic near verbatim** [web/src/pages/BootstrapForm.tsx, web/src/pages/LoginPage.tsx] — Reason: this story's own Dev Notes discourage over-abstracting a one-time flow, and no third consumer exists yet to justify extraction. Revisit if a third similar form appears (e.g. Story 1.5's password reset).

## Deferred from: code review of 1-1-administrator-login-session (2026-07-17)

- **`create_engine()`/`create_session_factory()`'s `lru_cache` singleton has no dispose/cache-clear path** [adapters/persistence/database.py:19-25] — Reason: inherent to this story's own Task 1 requirement to cache them as process-lifetime singletons. Only matters if a future story needs per-test event-loop isolation beyond the session-scoped test loop this story introduced.
- **`audit_log_entries` has no index beyond its primary key and no FK on `actor_user_id`** [alembic/versions/98ddc369b175_user_and_audit_log_entities.py] — Reason: not required by any AC. Revisit at real scale or when Story 5.2 builds the Audit Log screen.
- **`adapters/persistence/users.py` constructs `Role(row.role)`/`UserStatus(row.status)` unguarded against a value outside the enum** [adapters/persistence/users.py:31-37] — Reason: only reachable via external DB corruption; no code path in this diff (or any story so far) can write an invalid value.
- **`docker/nginx/nginx.conf`'s `/auth/` block doesn't match a bare `/auth` (no trailing slash), which falls through to the SPA** [docker/nginx/nginx.conf:51-56] — Reason: mirrors the pre-existing `/webhooks/` convention from Story 1.0; no app code path ever requests the bare path.

## Deferred from: code review of 1-3-role-scoped-portal-access-last-admin-guard (2026-07-17)

- **No audit entry for role-based authorization rejection at `get_current_user`** [api/auth/dependencies.py:53-54] — Reason: belongs to Story 1.4 (revocation). Login-time role rejection (`domain/auth.py`) writes a `login.failure` audit entry via `AuthenticationService.login`; per-request role rejection at the shared `get_current_user` dependency writes nothing — a previously-issued, still-valid token for a user whose role no longer qualifies is silently 401'd with no forensic record. Revisit when Story 1.4 designs session/token revocation and its audit trail.
- **`LastAdministratorGuard.ensure_can_deactivate` has a check-then-act race, no locking** [domain/administrators.py:260-266] — Reason: reads `count_active_administrators()` and decides in a separate step, with no row lock/`SELECT ... FOR UPDATE`/advisory lock (contrast with `acquire_bootstrap_lock`'s explicit `pg_advisory_xact_lock` for the same class of problem in Story 1.2). Two concurrent deactivate requests against the last two active Administrators could both read `count == 2`, both pass, and both proceed — leaving zero active Administrators, the exact failure FR-2 exists to prevent. Currently unreachable (guard has no caller yet), and per this story's own Dev Notes the locking/transaction design belongs to Story 3.1 ("that endpoint's real shape... is Story 3.1's job to design"). Revisit when Story 3.1 wires up the deactivate/delete endpoint.
- **`count_active_administrators` query has no index on `role`/`status` columns** [adapters/persistence/users.py] — Reason: pre-existing schema (columns from Story 1.1's migration `98ddc369b175`, no new migration needed for this story); low risk at expected table size. Revisit if this becomes a hot path at scale.

## Deferred from: code review of 1-4-session-invalidation-logout-revocation (2026-07-17)

- **`get_current_user` never checked `user.status`** [api/auth/dependencies.py:37-58] — Resolved by this story: `get_current_session` now rejects a deactivated Administrator's still-valid token with `account_deactivated` before it ever reaches `get_current_user`. Superseded, not carried forward.

## Deferred from: code review of 1-5-login-lockout-password-reset (2026-07-18)

- **`/auth/forgot-password` reopens the login-flow's timing side-channel** [domain/password_reset.py:55-86, api/auth/routes.py:246-276] — Reason: a valid username triggers extra DB work (INSERT token + INSERT audit row) before returning, unlike login's dummy-hash-equalized timing. Low exploitability for Phase-1 (admin-only, single-tenant, self-hosted deployment; precise timing measurement requires network access an external attacker is unlikely to have). Revisit if the deployment becomes multi-tenant or public-facing.
- **Password reset does not invalidate other already-issued JWT sessions for the account** [domain/password_reset.py:88-99] — Reason: deliberately out-of-scope per this story's own Dev Notes — no per-user session registry exists (`revoked_tokens` is keyed by individual `jti`, not by user); building bulk per-user revocation is out of scope for AD-11's schema and this story's ACs.
- **No rate limiting on `/auth/forgot-password` or `/auth/reset-password`** [api/auth/routes.py:246-295] — Reason: consistent with app-wide absence of throttling infrastructure; no rate-limiting middleware exists anywhere in this codebase today.
- **`increment_failed_login_count`'s `scalar_one()` would raise unhandled if the user row vanished mid-request** [adapters/persistence/users.py] — Reason: unreachable today; no user-deletion feature exists anywhere in the codebase (only test cleanup deletes user rows).
- **`password_reset_tokens.user_id` FK has no `ON DELETE` behavior** [alembic/versions/8ae7e5d0d8c9_login_lockout_and_password_reset.py] — Reason: only matters once a user-deletion feature exists; will hard-fail on any user with reset-token history at that point.
- **Concurrent simultaneous lockout-triggering requests can each independently call `lock_until` and write a duplicate `account.locked` audit row** [domain/auth.py:66-84] — Reason: narrow race window, results only in a duplicate audit row, no functional or security harm.
- **Requesting a new reset link does not invalidate prior outstanding tokens for the same user** [domain/password_reset.py:55-86] — Reason: not required by any AC; a defense-in-depth nicety beyond this story's scope.
- **`new_password`'s 72-character cap (not byte length) lets bcrypt's 72-byte truncation bite on multi-byte-character passwords** [api/auth/routes.py:70-72] — Reason: pre-existing pattern copied verbatim from `LoginRequest`/`BootstrapRequest` (Story 1.1), not newly introduced by this story.
- **`new_password` has no complexity/strength requirement** [api/auth/routes.py:70-72] — Reason: same pre-existing convention as every other password field in this app.
- **Frontend lockout countdown is seeded once and counts down client-side with no re-sync to the server** [web/src/pages/LoginPage.tsx:43-56] — Reason: cosmetic only; the server still enforces the real lockout independently, so clock drift can at worst re-enable the submit button slightly early, resulting in one extra rejected request.
- **`retry_after_seconds` is only in the JSON body, no standard `Retry-After` HTTP header** [api/auth/routes.py:97-105] — Reason: beyond what AC #1 requires; the body already carries the remaining seconds.
- **`PasswordResetService.token_ttl` is unused by `complete_reset`** (`reset_password` route passes a meaningless `timedelta(0)`) [domain/password_reset.py:41-53, api/auth/routes.py:286-288] — Reason: already flagged and accepted as "slightly awkward" in this story's own Dev Notes; a future refactor could split `token_ttl` into a `request_reset`-only parameter.

## Deferred from: code review of 1-6-design-system-foundation-shared-interaction-patterns (2026-07-18)

- **Alembic migration sets `server_default="system"` but `UserModel.theme_preference` has no matching `server_default`, so model metadata and live schema disagree** [alembic/versions/d4d9c5b96249_user_theme_preference.py:32, adapters/persistence/users.py:30] — Reason: not a regression introduced by this story; the same convention (Python-side `default=` only, no `server_default`) is already used by `failed_login_count`/`locked_until` since Story 1.1/1.5. A future `alembic revision --autogenerate` may propose dropping the hand-added server default; worth fixing across all three columns together rather than one at a time.
- **`_to_domain` calls `ThemePreference(row.theme_preference)` with no guard against an unrecognized DB value, which would raise an unhandled `ValueError`** [adapters/persistence/users.py:44] — Reason: extends the same unguarded pattern already deferred for `Role(row.role)`/`UserStatus(row.status)` in the 1-1 code review; only reachable via external DB corruption, no code path writes an invalid value today.

## Deferred from: code review of 2-3-brand-performance-analytics (2026-07-19)

- **Exactly-0% growth renders with the green "up" badge/arrow** [web/src/components/BrandPerformanceSection.tsx] — Reason: `Number(entry.growth_pct) >= 0` treats exactly-zero growth as "up" (success/green), with no neutral state. Not specified anywhere in Story 2.3's ACs or the design docs — a design call for a future story.

## Deferred from: code review of 2-4-prioritized-doctor-visit-list (2026-07-19)

- **`list_all()` has no transactional/snapshot coordination with an in-progress `upsert_many()` ingestion batch** [adapters/persistence/doctors.py:80] — Reason: a concurrent read during a nightly ingestion run could observe a partially-updated snapshot. Pre-existing architectural pattern shared with `BrandPerformanceService`/`DashboardMetricsService` (same read-without-locking design across the codebase), not introduced by this diff. Revisit at the ingestion-locking level in a future story if Epic 4's Daily Report generation needs stronger consistency guarantees.

## Deferred from: code review of 3-2-manage-recipient-groups-channels (2026-07-20)

- **No guard against acting on an already-inactive RecipientList (double-remove / edit-while-inactive)** [domain/recipients.py:355-411] — Reason: `remove_recipient_list` only checks the target isn't `None`, not that it's still `ACTIVE`; calling `DELETE` twice bumps `version` and writes a duplicate `recipient_list.deactivated` audit entry, and `PATCH` on an inactive list silently edits its fields without reactivating it. Mirrors the identical, already-existing gap in `TeamDirectoryService.remove_team`/`update_team` and `UserDirectoryService.remove_user`/`update_user` (Story 3.1) — not introduced by this diff. Revisit across all three directory entities together.
- **Whitespace-only name passes validation and can create a blank-named RecipientList** [domain/recipients.py:333-370] — Reason: Pydantic's `Field(min_length=1)` only checks raw character count, so a single-space name passes, then `.strip()` reduces it to `""` with no empty-after-strip check. Identical latent gap already present in `TeamDirectoryService.create_team`/`update_team`, not introduced by this diff. Revisit alongside the Team version of the same bug.

## Deferred from: code review of 3-1-manage-users-sales-teams (2026-07-20)

- **Optimistic-concurrency `version` column has no stale-write rejection on any update path** [domain/models.py, adapters/persistence/users.py:161-169, adapters/persistence/teams.py:83-89] — Reason: explicitly out of scope per this story's own Dev Notes and Completion Notes: Story 3.4 (Concurrent-Edit Conflict Detection) owns stale-write rejection; `version` is kept incrementing correctly so that story has real data to work against.
- **Alembic `downgrade()` requires manually deleting Sales User/Manager rows before it can succeed** [alembic/versions/dba27c6b09b6_recipient_directory_users_and_teams.py] — Reason: explicitly documented as an intentional, accepted consequence in the migration's own downgrade docstring per this story's Task 1 instructions.
- **No pagination on `GET /users`/`GET /teams`** [api/recipients/routes.py:199-208,305-311] — Reason: consistent with this story's explicit "never hide rows" requirement; a real future scaling concern but not this story's scope. Revisit once the directory grows large enough to matter.
- **No test for a non-sole Administrator deactivating their own account** [tests/api/test_recipients_routes.py] — Reason: coverage gap only; the guard behavior itself is correct and tested for the sole-admin case.
- **No client-side (or server-side) format validation on the `mobile` field** [api/recipients/routes.py:36-46] — Reason: needs a design decision (E.164? country-specific?) rather than guessing a format now; the directory's whole purpose is a WhatsApp-notification recipient list, so this matters but shouldn't be guessed at.

## Deferred from: code review of 3-3-recipient-opt-in-consent-capture (2026-07-20)

- **Consent grant/revoke handlers lack an unmount guard** [web/src/pages/UserFormDialog.tsx:95-137] — Reason: `handleSubmit` in the same component already lacks an `isMountedRef`-style guard (unlike `RecipientsPage.tsx`'s load functions, which do have one), so this is consistent with an existing gap in this file rather than a regression introduced by this diff.

## Deferred from: code review of 2-2-dashboard-summary-view (2026-07-20)

- **`list_all()`/`latest_per_team()` don't filter to active-only teams; an active + soft-deleted team can share a display name, colliding on the Team Performance tile's React key** [adapters/persistence/teams.py:80-83, adapters/persistence/sales_data.py:84-91] — Reason: `Team.status`/soft-delete didn't exist at this story's commit (b8fb965..7aad716); it was added afterward by Story 3.1, which never updated these Dashboard read paths to match its own `get_by_name`/`get_or_create_by_name` active-only convention. Real in current HEAD; needs a follow-up against Story 3.1's integration, not this story.

## Deferred from: code review of 4-1-compose-send-manual-notification (2026-07-21)

- **Manual send batch is dispatched synchronously in one request/DB transaction with no size cap** [domain/notifications.py:256-294] — Reason: explicitly, deliberately scoped to Story 4.2 per this story's own code comment/Dev Notes (NFR-1's 500+ concurrent-dispatch figure is 4.2's scope, not this story's). Revisit if Story 4.2's fan-out design doesn't end up superseding this synchronous loop for large recipient counts.
- **Silent recipient drop if `claim_for_dispatch` ever loses the race** [domain/notifications.py:261-263] — Reason: explicitly out of this story's scope per the spec — retry/claim-race handling belongs to Story 4.3; the code already documents (`# pragma: no cover`) and tests this branch as expected-unreachable within 4.1 (no concurrent writer exists yet). Revisit when Story 4.3 builds retry logic on top of `failed_retryable`.
- **No server-side idempotency/double-send protection beyond the frontend's `submitting` guard** [api/notifications/routes.py, domain/notifications.py] — Reason: not required by any AC in this story (AC #5 only requires the client-side single-flight guard, which is present and correct); a page refresh, second tab, or client retry can still produce a second independent `Notification` to the same recipients. Worth a design decision (idempotency key? notification-level dedupe window?) in a future story.

## Deferred from: code review of story-3-4-concurrent-edit-conflict-detection (2026-07-20)

- **Client-side "Keep My Changes" never updates local `version` state after a successful retry** [web/src/pages/RecipientListFormDialog.tsx, web/src/pages/TeamFormDialog.tsx, web/src/pages/UserFormDialog.tsx `handleKeepMine`] — Reason: `handleKeepMine` calls `performSave(conflict.version)` but never `setVersion(conflict.version)`. Currently masked because every caller closes the dialog on `onSaved` (state is discarded with the unmount), so no observable bug today. Revisit if a future caller ever keeps the dialog open after a successful save.
- **Deactivate races the edit-version-check with no active-status guard, and `ConflictDialog` never surfaces `status`** [domain/recipients.py `update_user`/`update_team`/`update_recipient_list`] — Reason: same root cause as the already-deferred "edit-while-inactive" gap from Story 3.2's review (`PATCH` on an already-soft-deleted record silently succeeds without reactivating it) — this story's own Dev Notes explicitly scope removal/deactivate as unguarded by design, since AC #1 only requires *edit-vs-edit* conflicts to be caught. A concurrent edit-vs-deactivate collision is still caught as a version conflict, but the `ConflictDialog` doesn't show `status`, so "Keep My Changes" can silently resurrect/rename a record someone just deactivated. Revisit alongside the 3-2 deferred item, across all three directory entities together.
- **`RecipientListFormDialog`'s `handleDiscardMine` doesn't reset `kind` on discard** [web/src/pages/RecipientListFormDialog.tsx:120-126] — Reason: `kind` is a fixed prop set by which tab opened the dialog, not local state; if the other party's version differs by `kind` too, discarding never picks up their `kind`. Currently unreachable since no UI exposes kind-editing on an existing list (the domain layer allows it, the frontend doesn't exercise it). Revisit if a future feature adds a kind-editing control.

## Deferred from: code review of 4-5-manage-message-templates (2026-07-22)

- **`TemplateFormDialog.tsx`'s "Cancel" button lacks `type="button"` inside `<form onSubmit={handleSubmit}>`** [web/src/pages/TemplateFormDialog.tsx:177] — Reason: copied verbatim from `TeamFormDialog.tsx`, which has the identical gap and which this story was explicitly instructed to mirror closely — pre-existing pattern, not introduced by this diff. Revisit alongside a fix to `TeamFormDialog.tsx`'s (and any other form dialog's) same issue, rather than fixing it once here.
- **New unique index on `message_templates.name` has no pre-migration duplicate-name cleanup** [alembic/versions/b3f7a1c9d2e4_message_templates_name_unique.py:20-29] — Reason: no demo/prod environment currently has duplicate template names, so accepted as a deploy risk rather than adding a data-cleanup migration step now. Revisit (add a dedupe/rename step) if a future environment's `message_templates` table ever has colliding names before this migration runs.

## Deferred from: code review of 4-3-delivery-status-webhook-automatic-retry (2026-07-22)

- **`list_retry_eligible` has no `LIMIT`, ordering, or row-locking** [adapters/persistence/notifications.py:339-359] — Reason: `claim_for_dispatch`'s atomic conditional UPDATE still prevents double-dispatch, so this isn't a correctness gap, only a scalability one. A large failure backlog (e.g. a Twilio outage) is fetched and dispatched serially within a single ~30s poll cycle with only APScheduler's `max_instances=1` as backpressure. Not required by any AC. Revisit if retry volume ever approaches a scale where a single poll cycle's serial dispatch becomes a real bottleneck.

## Deferred from: code review of 4-2-automated-daily-report-generation-send (2026-07-22)

- **Broad `except Exception` around `bulk_create` also silently mislabels any unrelated DB failure (a broken FK, a transient connection error) as "duplicate skip, not an error"** [domain/scheduled_notifications.py:141-157] — Reason: same shape as `domain/ingestion.py`'s own broad-except precedent this story deliberately mirrors (AD-1 forbids `domain/` from importing sqlalchemy, so the except clause can't be narrowed to the specific constraint-violation type). Not a regression introduced by this story specifically.
- **Single DB transaction spans the full live Twilio dispatch loop at company-wide fan-out scale, not just a small ad hoc batch** [domain/scheduled_notifications.py, domain/notifications.py:dispatch_deliveries] — Reason: inherited unchanged from Story 4.1's `ManualNotificationService.compose_and_send` transaction shape (this story's Task 4 Step 8 explicitly required preserving that exact behavior). NFR-1's 500+-concurrent-dispatch scaling question is already explicitly deferred past this story in the story's own Dev Notes.

## Deferred from: code review of 4-4-daily-report-schedule-configuration (2026-07-23)

- **`web/vite.config.ts`'s bare `'/settings'` proxy prefix will also intercept direct navigation/refresh to the `/settings` SPA route in dev** [web/vite.config.ts:22] — Reason: pre-existing pattern already present for `'/notifications'` (same prefix-vs-SPA-route ambiguity), not a regression introduced uniquely by this diff.
- **`SettingsPage.tsx`'s load/save error handling treats 401 the same as any other failure (generic inline error) instead of redirecting to login** [web/src/pages/SettingsPage.tsx] — Reason: matches `TemplatesPage.tsx`'s existing convention; a codebase-wide gap, not specific to this story.
- **`report_schedules` table has no DB-level CHECK constraint on `send_hour_utc`/`send_minute_utc` ranges** [alembic/versions/7d3e9c2a5f81_report_schedule.py] — Reason: domain (`ReportScheduleService.update_schedule`) and API (`ReportScheduleWriteRequest`'s `Field(ge=..., le=...)`) layers already validate on every write path; a defense-in-depth gap only, not reachable via any existing code path.
- **Migration seed and `tests/conftest.py`'s reset both hardcode a literal `2026-07-23` timestamp rather than a computed one** [alembic/versions/7d3e9c2a5f81_report_schedule.py, tests/conftest.py] — Reason: a documented trade-off in the story's own Completion Notes — `op.bulk_insert`'s executemany-style parameter binding doesn't reliably support per-row SQL expressions like `sa.func.now()`.
- **The new Settings page isn't cross-linked from `TemplatesPage`/`NotificationComposePage` (only reachable via Dashboard)** [web/src/pages/DashboardPage.tsx] — Reason: consistent with the codebase's still-absent shared nav shell (explicitly flagged as "unowned by any story" in `TemplatesPage.tsx`'s own comment), not a regression unique to this diff.
- **Migration `downgrade()` drops `report_schedules` with no note that `scheduler/main.py`'s `daily_report` job would then fail every poll tick** [alembic/versions/7d3e9c2a5f81_report_schedule.py] — Reason: matches the same lack-of-coordination-note convention across every existing migration in this repo (none of them cross-reference downgrade-safety against dependent application code).
