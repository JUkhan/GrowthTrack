---
baseline_commit: 59ba1bac61ee0f2373ff263554ace4238156a35c
---

# Story 4.2: Automated Daily Report Generation & Send

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As an Administrator,
I want every configured Recipient to automatically receive a correctly formatted Daily Report once a day,
so that the team stays informed without anyone manually triggering it.

## Acceptance Criteria

1. **Given** the scheduled trigger time arrives (once per operational day) **When** the scheduler fires **Then** a Scheduled Notification is created, targeting all configured Recipients, and generation completes within 60 seconds

2. **Given** the Daily Report content **When** it is generated **Then** it matches `sample-whatsapp-report.md`'s format and figures (YTD/MTD sales, Achievement %, Growth %, team performance, condensed top/focus brand and doctor names, Cr BDT currency formatting) sourced from Epic 2's computed data

3. **Given** a Recipient reachable via more than one targeting mechanism (individual + Team, e.g.) **When** recipients are resolved for the day's run **Then** they are de-duplicated to exactly one `NotificationDelivery` row, keyed by `(recipient_user_id, operational_day)`

4. **Given** the Send Event uniqueness constraint **When** the scheduler restarts or fires twice for the same operational day **Then** no duplicate `NotificationDelivery` rows are created — the partial unique index rejects the duplicate

5. **Given** delivery begins **When** each recipient's send completes **Then** it lands in Notification History tagged "Scheduled" within 5 minutes of the scheduled time **And** the Dashboard's notification-status field (Story 2.2) reflects this run's outcome once it is the most recent send system-wide

## Tasks / Subtasks

- [ ] **Task 1: Schema changes** (AC: #1, #3, #4)
  - [ ] New Alembic migration, `down_revision = 'c4a8f21e6b3d'` (current head, Story 4.1's migration). `notifications.created_by_user_id` becomes **nullable** — `op.alter_column("notifications", "created_by_user_id", nullable=True)`, matching `dba27c6b09b6_recipient_directory_users_and_teams.py`'s exact `op.alter_column(..., nullable=True)` precedent for the identical kind of change (Story 3.1's `users.username`/`hashed_password`). **Why this is required, not optional:** a Scheduled Notification has no human actor — it is a system-triggered background job, exactly like Story 2.1's nightly import (`domain/ingestion.py`'s own docstring: "A nightly import has no actor... doesn't fit the Audit Log's shape or purpose"). The column is currently `nullable=False` with an FK to `users.id` (`adapters/persistence/notifications.py:57-59`); Task 4's `ScheduledReportService` cannot satisfy a NOT NULL constraint with no Administrator to attribute the send to, and must not invent a fake actor. No other column changes needed — the two partial unique indexes already created by Story 4.1's migration (`(notification_id, recipient_user_id) WHERE notification_type='manual'` and `(recipient_user_id, operational_day) WHERE notification_type='scheduled'`) already exist and already satisfy AC #3/#4 exactly as written; do not recreate or alter them.
  - [ ] Update `domain/models.py`: `Notification.created_by_user_id: uuid.UUID | None = None` (was non-optional). Current field order (`domain/models.py:200-206`) is `id, notification_type, template_id, created_by_user_id, created_at` — Python dataclasses reject a defaulted field followed by a non-defaulted one, so `created_by_user_id` must move to be the **last** field (after `created_at`), not just gain `= None` in place. Safe to reorder: every construction site (`domain/notifications.py`'s `compose_and_send`, `adapters/persistence/notifications.py`'s `_notification_to_domain`) already uses keyword arguments, not positional, so field-declaration order doesn't affect them.
  - [ ] Update `adapters/persistence/notifications.py`: `NotificationModel.created_by_user_id` becomes `Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=True)`; `_notification_to_domain` and `SqlAlchemyNotificationRepository.add` already pass the field through unchanged (both are already `None`-safe since they just forward the attribute) — verify, don't assume.
  - [ ] Add `DAILY_REPORT_LOCK_KEY = <new fixed 32-bit int>` to `adapters/persistence/advisory_locks.py`, following the file's own registry convention (a comment naming which story/purpose owns the key, and a value that doesn't collide with `BOOTSTRAP_LOCK_KEY`/`NIGHTLY_IMPORT_LOCK_KEY`/`ADMINISTRATOR_REMOVAL_LOCK_KEY`).

- [ ] **Task 2: Ports** (AC: #1, #4)
  - [ ] Extend `ports/notifications.py`'s `NotificationRepository` with `try_acquire_daily_report_lock() -> bool`, following `ports/import_runs.py`'s `try_acquire_lock()` docstring/shape exactly (non-blocking, transaction-scoped, returns `False` — without mutating anything — when another run already holds it). This is a **defense-in-depth** guard against two scheduler processes (or a mid-run restart) overlapping within the same trigger window — distinct from, and in addition to, AD-2's partial-unique-index guard (Task 4 below), which is the authoritative zero-duplicate-send mechanism per AD-2's own text ("the partial unique index rejects the duplicate"). The lock narrows the window in which that index would ever actually be hit; it is not a substitute for it.

- [ ] **Task 3: Persistence adapter** (AC: #1, #4)
  - [ ] Implement `try_acquire_daily_report_lock` in `SqlAlchemyNotificationRepository` (`adapters/persistence/notifications.py`), copying `SqlAlchemyImportRunRepository.try_acquire_lock`'s exact `pg_try_advisory_xact_lock` pattern (`adapters/persistence/import_runs.py:41-52`) with `DAILY_REPORT_LOCK_KEY` in place of `NIGHTLY_IMPORT_LOCK_KEY`.

- [ ] **Task 4: Domain — report content assembly + send orchestration** (AC: #1, #2, #3, #4, #5)
  - [ ] **New file `domain/report_formatting.py`** — Python port of `web/src/utils/format.ts`'s `formatCrBdt`/`formatPercent`, byte-for-byte matching rounding behavior (`round(value / Decimal("1e7"), 1)` formatted to exactly one decimal place; `round(value)` formatted with no decimal for percent) — **read `web/src/utils/format.ts` first**; its own comment states explicitly: "the Dashboard (Story 2.2) and the Daily Report (Story 4.2) must never disagree on these figures, so the formatting logic lives in one place" — that "one place" is TypeScript, which the Python-generated WhatsApp text cannot import, so this file is the other half of that stated intent, not a new invention. One divergence from the Dashboard's own suffix, required by the reference format: `sample-whatsapp-report.md` shows `"100 Cr BDT"` (with the `BDT` currency code), while `formatCrBdt`'s Dashboard usage renders bare `"X.X Cr"` — the **numeric rounding must be identical**, but this file's formatter appends `" Cr BDT"`, not `" Cr"`, to match the sample's literal reference text. Percent formatting has no such divergence (`"40%"`, no decimal, matches both contexts).
  - [ ] **New file `domain/daily_report.py`** — `DailyReportContentService`, composing the three already-existing Epic 2 read services with zero changes to any of them:
    - Depends on `DashboardMetricsService` (`domain/metrics.py`), `BrandPerformanceService` (`domain/metrics.py`), `DoctorVisitListService` (`domain/metrics.py`), `TeamRepository` (for `list_all()`'s `(id, name)` pairs — territory lookup, see below).
    - `build_company_wide_content(today: date, now: datetime) -> CompanyWideReportContent` — calls `DashboardMetricsService.get_summary(today, now)` and `BrandPerformanceService.get_summary()` **exactly once per scheduled run** (not once per recipient — these figures are identical for every Recipient; only the doctor-list section varies per-recipient). Returns YTD/MTD/Achievement%/Growth% (formatted via `report_formatting.py`), team-performance lines, and the single top-ranked Top Brand name and single top-ranked Focus Brand name (`BrandPerformanceSummary.top_brands[0]`/`.focus_brands[0]`, condensed to `brand_name` only — `[0]` may not exist if a list is empty; handle explicitly, see the empty-list Dev Notes item below). "Condensed to names only" is PRD `prd.md` line 172's own phrase, verbatim — full Sales/Rank/Growth stays Dashboard-only, never sent via WhatsApp.
    - `build_doctor_section(territory: str) -> str` — calls `DoctorVisitListService.get_visit_list(territory)`, takes the top `settings.report_top_doctors_n` entries (already priority-sorted ascending by the existing `_rank_doctors_for_territory`), joins their `doctor_name`s (see the no-newline Dev Notes item below for the join separator).
    - **`[ASSUMPTION — territory-to-Recipient mapping]`**: neither `entities.md`, the PRD, nor the Architecture spine defines how a Recipient's `Territory` (which scopes the Doctor visit list per `prd.md`'s own Glossary: "Territory — scopes both team performance and the Doctor visit list") is determined from a `User` row — `Team` (`domain/models.py:117-122`) has no `territory` field, and `User` has only `team_id`. This function resolves it as: look up the recipient's `Team.name` via `team_id` (using `TeamRepository.list_all()`'s existing `(id, name)` map, the same lookup `DashboardMetricsService` already performs), then match it against `Doctor.territory` case-insensitively (`.strip().lower()`, mirroring `_rank_doctors_for_territory`'s own existing normalization at `domain/metrics.py:271`). This is not a guess invented for this story — `scripts/seed_demo_data.py`'s own `_seed_doctors` (line 233: `for territory in team_ids:`) already seeds `Doctor.territory` as exactly the Team name string (`"North Region"`, `"South Region"`, etc.), confirming `Team.name == Territory` is this codebase's own established convention, just never previously stated as a rule anywhere in the domain layer. A Recipient with `team_id is None` (shouldn't occur for an active Sales User/Manager per `scripts/seed_demo_data.py`'s roster shape, but not schema-enforced) gets an explicit fallback string, not a silent blank — see the edge-case Dev Notes item.
  - [ ] **New file `domain/scheduled_notifications.py`** (or add to `domain/notifications.py` — dev agent's choice; a new file keeps `domain/notifications.py` from growing past ~500 lines, but if kept together, group under a clearly-commented section) — `ScheduledReportService.run_daily_report(today: date, now: datetime) -> ScheduledReportResult`:
    1. `try_acquire_daily_report_lock()` — if `False`, log "scheduled report already running, skipping" and return an outcome equivalent to `ImportOutcome.SKIPPED` (mirror `domain/ingestion.py`'s `SKIPPED` semantics/naming exactly — same reasoning, same shape, this codebase already has the vocabulary for "another run holds the lock, this is not an error").
    2. Resolve all sendable Recipients: `all_users = await self._users.list_all()`; filter to `status == UserStatus.ACTIVE` client-side, then call the **existing, unmodified** `RecipientResolutionService.resolve(user_ids=[u.id for u in active_users], team_ids=[], recipient_list_ids=[])` (AD-2: "one resolution function... two callers" — this makes three: composer preview, Manual send, and this. Team/RecipientList expansion is irrelevant here since every active User is already being passed individually — passing `team_ids=[]`/`recipient_list_ids=[]` is correct, not an oversight). Reuses the exact same active/mobile/consent filtering Story 4.1 already built and tested — no new filtering logic. If `resolved.unique_count == 0`, log a warning (mirror `domain/ingestion.py`'s "zero records... logged, not treated as an error" pattern at `domain/ingestion.py:249-254`) and return cleanly — **do not raise** `NoRecipientsSelected` (that exception's 422-mapping is Manual-composer-specific, wrong for an unattended background job with nobody to show an error to).
    3. Call `DailyReportContentService.build_company_wide_content(today, now)` once.
    4. Load the "Daily Report" `MessageTemplate` by name (`MessageTemplateRepository.get_by_name`, already exists — Task 6 seeds this row) — if missing, log an error and return cleanly (an unseeded/unprovisioned template is an ops/config problem, not something to crash the scheduler process over, consistent with `scheduler/main.py:_run_nightly_import`'s top-level "never let this kill the scheduler process" wrapper).
    5. Create one `Notification` (`notification_type=SCHEDULED`, `created_by_user_id=None`, `template_id=<Daily Report template's id>`) with an **empty** `targets` list — see the Dev Notes item on why Scheduled Notifications carry no `NotificationTarget` rows (`NotificationRepository.add` already accepts `targets: list[Any]`, an empty list is not a new code path). No audit-log entry either — same system-job reasoning as `domain/ingestion.py`'s own explicit precedent (no human actor, structured logging instead).
    6. Bulk-create one `NotificationDelivery` per resolved recipient, `status=QUEUED`, `operational_day=today`, `notification_type=SCHEDULED` — **wrap this call in `try`/`except IntegrityError`**: a same-day duplicate run that slipped past the advisory lock (e.g. a scheduler container restart between the lock's commit and a cron misfire re-trigger) hits AD-2's partial unique index here; catch it, roll back, log "scheduled report already sent for operational_day {today}, skipping" and return — this is AD-2's own stated defense ("the partial unique index rejects the duplicate"), and a caught-and-logged skip is the correct behavior for AC #4, not an unhandled crash.
    7. Build per-recipient `content_variables`: everything from step 3 is identical across recipients except the doctor-names slot, which calls `build_doctor_section(territory)` once per **distinct** territory represented among this run's resolved recipients (not once per recipient — cache by territory string, several recipients typically share one Team/territory) via step 4's `DailyReportContentService`.
    8. Dispatch: **extract the existing per-recipient claim/send/fail loop out of `ManualNotificationService.compose_and_send`** (`domain/notifications.py:277-330`) into a shared private helper — e.g. `_dispatch_deliveries(self, delivery_rows, content_variables_by_recipient: dict[uuid.UUID, dict[str,str]], template, recipients_by_id)` — used by both `ManualNotificationService` (passing the *same* `content_variables` dict for every recipient, wrapped to fit the new per-recipient-dict shape) and `ScheduledReportService` (passing genuinely different per-recipient dicts, from step 7). Do not duplicate the claim-for-dispatch/try-except-fail/outcome-recording logic a second time — it is non-trivial (atomic claim, broad exception guarding per `_fail`'s own docstring) and any divergence between two independent copies is a latent bug. Preserve `ManualNotificationService`'s current external behavior and all of its existing tests unchanged.
  - [ ] `AC #1`'s "generation completes within 60 seconds" applies to steps 1-7 (DB reads/writes: resolving recipients, building content, creating rows) — not to full WhatsApp dispatch completion, which is FR-6's separate "delivery within 5 minutes" budget (AC #5). This mirrors `epics.md`'s own two-clause phrasing exactly ("generation completes within 60 seconds... lands in Notification History... within 5 minutes") — do not conflate the two into one deadline, and do not restructure the dispatch loop into async/concurrent fan-out to hit an artificially-tightened combined budget; the existing synchronous per-recipient loop (inherited from Story 4.1, Twilio's 80 msg/sec default rate limit) comfortably fits both real budgets at current seed-data scale (16 roster Users). NFR-1's 500+-concurrent-dispatch figure remains explicitly deferred past this story, exactly as Story 4.1's own Dev Notes already deferred it — this story does not resolve that scaling question either.

- [ ] **Task 5: Scheduler wiring** (AC: #1)
  - [ ] `config.py`: add `report_send_cron_hour: int = Field(default=1, ge=0, le=23)` / `report_send_cron_minute: int = Field(default=0, ge=0, le=59)` (UTC) — default corresponds to **01:00 UTC = 07:00 Asia/Dhaka**, which is materially more concrete than Story 2.1's own nightly-import placeholder: the PRD states this exact default explicitly (`prd.md` line 176: `"the schedule is... [ASSUMPTION: default 07:00 Asia/Dhaka, pending confirmation — §13]"`, repeated at line 357 and in the implementation-readiness report), not an engineering guess invented for this story — still flagged `[ASSUMPTION — CONFIRM]` in the field's docstring because the PRD itself marks it pending confirmation, same convention as `nightly_import_cron_hour`'s existing docstring. Also add `report_top_doctors_n: int = Field(default=5, gt=0)` — no source document states a doctor-list truncation count for the report (the sample shows 3, but `sample-whatsapp-report.md`'s own note frames it as illustrative, not a spec'd count); default 5 for consistency with `brand_top_n`'s existing default and docstring-flag it the same `[ASSUMPTION — CONFIRM]` way.
  - [ ] **Scope decision, not this story's gap to close**: `ReportSchedule` (AD-11, a DB-backed, Administrator-editable, no-redeploy-required singleton) is explicitly bound to **Story 4.4** in `ARCHITECTURE-SPINE.md`'s own AD-11 binding list ("epics.md Story 1.5, Story 1.6, **Story 4.4**"), not this story. Building the `ReportSchedule` table/repository/domain-service now, with no UI or admin-facing mutation path to write to it (Story 4.4 doesn't exist yet), would be dead infrastructure this story has no consumer for — the same over-scoping this codebase's own conventions avoid elsewhere. Use the identical `Settings`-field-plus-`.env`-escape-hatch pattern `nightly_import_cron_hour`/`nightly_import_cron_minute` already established for an equally-provisional trigger time (`config.py:64-70`), not a new mechanism. Story 4.4 will replace this env-var read with the DB-backed entity later — that migration is Story 4.4's job, not a regression introduced here.
  - [ ] `.env.example`: add `REPORT_SEND_CRON_HOUR=1` / `REPORT_SEND_CRON_MINUTE=0` and `REPORT_TOP_DOCTORS_N=5` with a comment block matching `NIGHTLY_IMPORT_CRON_HOUR`/`_MINUTE`'s existing style exactly (lines 53-57) — state the UTC/Asia/Dhaka equivalence and the `[ASSUMPTION — CONFIRM]` flag. Do **not** add these to `docker/docker-compose.yml`'s `x-backend-env` anchor — `nightly_import_cron_hour`/`_minute`/`dashboard_stale_after_hours`/`brand_top_n`/etc. are *not* in that anchor either (only fields with no usable default, or secrets, are), so a containerized scheduler already silently runs on `Settings`' Python-level defaults for all of these regardless of `.env` — a pre-existing, out-of-scope-for-this-story characteristic of the nightly-import precedent this story is deliberately mirroring, not a new gap to fix here.
  - [ ] `scheduler/main.py`: add a new job registration in `_register_jobs`, mirroring `_run_nightly_import`/`_run_nightly_import_async`'s exact two-function shape (sync wrapper catching all exceptions and logging via `logger.exception(...)`, calling an inner `async def` that opens its own session, constructs every repo/service, calls `ScheduledReportService.run_daily_report(today, now)`, commits). Compute `today`/`now` the same way `api/dashboard/routes.py:50` and `scripts/seed_demo_data.py:272` already do: `now = datetime.now(UTC)`, `today = now.astimezone(ZoneInfo("Asia/Dhaka")).date()` — this **is** the Operational Day per PRD Glossary ("a calendar day in Asia/Dhaka time"), and is what gets written to `NotificationDelivery.operational_day`. `scheduler.add_job(_run_daily_report, "cron", hour=settings.report_send_cron_hour, minute=settings.report_send_cron_minute, id="daily_report")`.

- [ ] **Task 6: Seed data** (AC: #2)
  - [ ] Extend `scripts/seed_demo_data.py`'s `_seed_message_templates` (or add a sibling `_seed_daily_report_template`) to insert a second demo `MessageTemplate` row named exactly `"Daily Report"` (the name `ScheduledReportService` looks up via `get_by_name`), placeholder `twilio_content_sid`, and `variable_slots` covering every field Task 4 assembles (ytd_sales, mtd_sales, achievement_pct, growth_pct, team_performance, top_brand, focus_brand, top_doctors — exact slot names at the dev agent's discretion, matching `DailyReportContentService`'s output field names for traceability). Document inline, same as the existing template's comment, that the real Content SID must be swapped in from the Twilio Console before any real send, **and** that this template's *Meta-approved* body text must itself be authored to avoid embedded newlines/4+ consecutive spaces inside any parameter placeholder (see the WhatsApp-template-constraints Dev Notes item — this is a content-authoring constraint on the *actual* Twilio Console template, which this seed script cannot enforce, only document).

- [ ] **Task 7: Tests** (AC: all)
  - [ ] `tests/domain/test_daily_report_content.py` — `DailyReportContentService`: company-wide figures computed once and identical across a multi-recipient run (not recomputed per recipient — assert the underlying `DashboardMetricsService`/`BrandPerformanceService` fakes are called exactly once regardless of recipient count); territory-to-doctor-list resolution (Team-name-to-Doctor.territory case-insensitive match, including a Team name with different casing/whitespace than the stored `Doctor.territory`); empty `top_brands`/`focus_brands`/doctor-list handling (no `IndexError`, an explicit "no data" fallback string instead); `report_formatting.py`'s Cr BDT/percent output matches specific known input/output pairs computed by hand from `web/src/utils/format.ts`'s formula (cross-reference, not just "some string comes out").
  - [ ] `tests/domain/test_scheduled_notifications_service.py` — hand-written `Fake*Repository` classes, same style as `tests/domain/test_notifications_service.py`: happy path (N active/consented Users, each resolves to exactly one `NotificationDelivery`, `operational_day` set, `notification_type=SCHEDULED`, `Notification.created_by_user_id is None`, zero `NotificationTarget` rows created); overlap across two Users sharing the same territory (asserts `build_doctor_section` isn't recomputed per-recipient — a call-count assertion on the fake `DoctorRepository`, not just correct output); zero eligible recipients (completes cleanly, no exception, no rows created); advisory-lock-held case (second concurrent call returns the skipped outcome, doesn't touch delivery rows); a fake that raises the DB unique-constraint IntegrityError shape on the second `bulk_create` call to verify the catch-and-skip path (AC #4) without needing real Postgres for this particular case.
  - [ ] `tests/adapters/persistence/test_notifications_repository.py` (extend, don't replace) — real Postgres: verify `try_acquire_daily_report_lock` transaction-scoped behavior (second concurrent acquire in a separate connection returns `False` until the first transaction ends), verify a second real `bulk_create` for the same `(recipient_user_id, operational_day)` genuinely raises the expected `IntegrityError` from the *actual* partial unique index (not just the domain-level fake) — this is the one thing only a real-DB test can confirm, AC #4's real backstop.
  - [ ] `tests/api/test_dashboard_routes.py` — extend the existing notification-status test(s) to also cover a `Scheduled`-type latest Notification (the read path, `most_recent_status_summary`, is `notification_type`-agnostic already — confirm this with an explicit test rather than relying on it being incidentally true).
  - [ ] Update `tests/conftest.py`'s `_clean_tables` **only if** Task 1 adds any new table — this story's migration only alters an existing column and adds no new table, so no `_clean_tables` change should be needed; verify this assumption rather than skip the check.

## Dev Notes

- **This story is a lateral extension of Story 4.1's already-built hexagonal slice, not a greenfield one.** `Notification`/`NotificationDelivery`/`NotificationTarget`/`MessageTemplate` domain types, ports, adapters, and the two AD-2 partial unique indexes already exist and already correctly support `notification_type=scheduled` — Story 4.1's own migration deliberately built both index variants up front. The genuinely new work is: (a) making `created_by_user_id` nullable, (b) assembling Daily Report *content* from three existing, untouched Epic 2 read services, (c) an advisory-lock + unique-index-catch idempotency path distinct from Manual's picker-driven flow, and (d) scheduler wiring. Do not re-derive or duplicate anything Story 4.1 already built (recipient resolution, atomic claim, Twilio adapter, dashboard status tile) — extend it.
- **WhatsApp template parameters cannot contain newline characters, tabs, or 4+ consecutive spaces** (current Meta WhatsApp Business Platform template policy, verified 2026-07-21 — see Sources below) — this directly conflicts with `sample-whatsapp-report.md`'s literal multi-line layout (one team per line, one doctor per line) if a naive design tried to pack the whole "Team A : 45% / Team B : 50% / Team C : 40%" block into a single template variable with embedded `\n` characters. This is a real current platform constraint, not a stylistic choice. **Design decision for this story**: `team_performance` and `top_doctors` content-variable values use a non-newline separator (e.g. `"; "`) between entries instead of literal newlines — preserves all the same information (team names, percentages, doctor names, in the same order) but cannot reproduce the sample's exact line-broken visual layout inside a single Content API variable. This is a genuine, `[ASSUMPTION — CONFIRM]`-worthy divergence from a literal reading of AC #2 ("matches `sample-whatsapp-report.md`'s format") — the *figures and information* match; the *literal line-break layout* cannot, given the platform constraint discovered above. Flag this explicitly rather than silently picking a separator and calling AC #2 fully satisfied. An alternative some teams use — a fixed maximum number of individually-numbered slots (`team_1_line`, `team_2_line`, ... up to some N, blank-padded) — was considered and rejected: Meta also disallows empty-string parameter values, and the number of Teams is data-driven (currently 4 in demo seed data, not fixed by any spec), so a fixed-slot design would need silent truncation or a template redesign the moment a 5th Team is added — a separator-joined single variable scales to any Team/doctor count without a template change.
- **Territory-to-Recipient mapping (`Team.name == Doctor.territory`) is this story's own inferred convention**, not stated in any planning document as a rule — see Task 4's `[ASSUMPTION]` note for the full reasoning and the `scripts/seed_demo_data.py` evidence backing it. If a future Source System integration (AD-6, still unidentified per PRD Open Question #1) ever ingests `Team`/`Doctor` data where team names and territory strings diverge, this mapping silently breaks (empty doctor sections, not an error) — worth a code comment at the match site pointing back to this story for whoever debugs that later.
- **No portal UI work in this story** — unlike Story 4.1's Task 6/7 (composer page, Dashboard wiring), Story 4.2 has zero frontend tasks. The Dashboard's notification-status tile (`web/src/pages/DashboardPage.tsx`, wired in 4.1) already reads `GET /dashboard/notification-status`, which already reads `most_recent_status_summary()` ordered by `Notification.created_at` system-wide, `notification_type`-agnostic — a Scheduled run's outcome will show up there automatically with no frontend change, per AC #5's second clause. Story 2.4's own precedent (doctor visit list shipped with zero portal screen, verified only via domain/repository tests) applies again here for the same reason: no Daily-Report-viewing UI exists or is planned (Notification History is Epic 5 Story 5.1, not yet built) — AC #5's "lands in Notification History" is, like Story 4.1's AC #7, only satisfiable at the data layer in this story (correct `notification_type='scheduled'` tagging, queryable later).
- **No audit-log entry for a Scheduled run** — mirrors `domain/ingestion.py`'s explicit, already-justified precedent (system-triggered job, no human actor, AD-7's co-transactional rule governs *administrative actions*, not background jobs) almost exactly. Use structured JSON logging with a `correlation_id` per run instead, same shape as `domain/ingestion.py`'s `_run_pipeline` logging (`extra={"correlation_id": ..., ...}`) — `scheduler/main.py`'s `_JsonFormatter` already renders this correctly for any `extra=` fields, no logging-infra change needed.
- **Two distinct idempotency mechanisms, layered, not redundant**: the new advisory lock (Task 2/3) narrows the *concurrency* window (two processes/threads racing within the same trigger); the pre-existing AD-2 partial unique index (Story 4.1's migration) is the *authoritative* zero-duplicate-send guarantee for the case the lock doesn't fully cover (a scheduler container restart between the lock transaction's commit and a subsequent misfire re-trigger for the *same* operational day, potentially minutes or hours apart — well outside any single transaction's lock lifetime). Task 4 Step 6 must catch the resulting `IntegrityError` and treat it as an expected, loggable no-op — an *unhandled* crash on that path would technically still prevent the duplicate row (satisfying AC #4's literal text) but would leave the scheduler job failed/logged-as-an-error for a case that isn't actually an error.
- **Twilio dispatch-loop extraction (Task 4 Step 8) is a real, non-trivial refactor** of `domain/notifications.py:277-330` — read `ManualNotificationService.compose_and_send`'s existing implementation and its accompanying tests (`tests/domain/test_notifications_service.py`) fully before touching it. The claim-for-dispatch / broad-exception-guard / outcome-recording shape has its own code-review history (see Story 4.1's Review Findings — the "uncaught exceptions during dispatch can lose already-sent messages" patch) — preserve that exact behavior for both callers, don't regress it while extracting.

### Current WhatsApp Business Platform specifics (verified 2026-07-21, not just training data)

- Template body **parameter values** (the strings passed as `content_variables`) cannot contain newline characters, cannot contain more than 4 consecutive spaces, and cannot themselves start or end a template body. This governs the `team_performance`/`top_doctors` content-variable design decision above.
- Full URLs are disallowed inside body placeholders (anti-phishing) — not directly relevant to this story's fields (no URLs in the Daily Report), noted for completeness since it's a related constraint on the same parameter surface.
- These are Meta WhatsApp Business Platform template-approval rules, enforced at Meta's template-review stage in the Twilio/Meta console (the same out-of-scope-for-code approval process Story 4.1's Dev Notes already named for `MessageTemplate`) — nothing in this story's Python code can violate them since the code only ever supplies `content_variables` values, never the template body itself; the risk is entirely in how the *actual* Twilio Console "Daily Report" template gets authored, which this story's seed-script comment (Task 6) can only flag, not enforce.

Sources:
- [WhatsApp template message guidelines: naming, formatting, and translations | Wati.io Help Center](https://support.wati.io/en/articles/11463458-whatsapp-template-message-guidelines-naming-formatting-and-translations)
- [How to avoid too many parameters rejection in WhatsApp Templates – Syniverse Developer Community](https://sdcsupport.syniverse.com/hc/en-us/articles/30569685832471-How-to-avoid-too-many-parameters-rejection-in-WhatsApp-Templates)

### Project Structure Notes

New files:
`domain/report_formatting.py`, `domain/daily_report.py`, `domain/scheduled_notifications.py` (or merged into `domain/notifications.py`, dev agent's choice per Task 4), `alembic/versions/<new>_notifications_created_by_nullable.py`, `tests/domain/test_daily_report_content.py`, `tests/domain/test_scheduled_notifications_service.py`.

Modified files: `domain/models.py` (`Notification.created_by_user_id` optional), `adapters/persistence/notifications.py` (nullable column + new `try_acquire_daily_report_lock`, plus the extracted shared dispatch helper if factored into this layer's caller rather than staying in `domain/notifications.py` — it belongs in `domain/notifications.py`, not here, per AD-1), `adapters/persistence/advisory_locks.py` (new key), `ports/notifications.py` (`try_acquire_daily_report_lock` on `NotificationRepository`), `domain/notifications.py` (`ManualNotificationService`'s dispatch loop extracted to a shared helper — behavior-preserving), `config.py` (`report_send_cron_hour`/`_minute`, `report_top_doctors_n`), `.env.example`, `scheduler/main.py` (new job registration), `scripts/seed_demo_data.py` (Daily Report template seed), `tests/domain/test_notifications_service.py` (update for the extracted-helper refactor if its internals are asserted against directly — check before assuming no change needed), `tests/adapters/persistence/test_notifications_repository.py`, `tests/api/test_dashboard_routes.py`.

No conflicts with the fixed source tree — this story adds to `domain/`/`adapters/persistence/`/`ports/`/`scheduler/` exactly as the Architecture Spine's Capability → Architecture Map already names for CAP-3 (`scheduler/`, `domain/notifications`, `adapters/whatsapp_twilio`).

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 4.2: Automated Daily Report Generation & Send] — story statement, all 5 ACs, FR-6
- [Source: ...ARCHITECTURE-SPINE.md#AD-2] — Send Event identity for Scheduled = `(recipient_user_id, operational_day)`, partial unique index, fresh-at-trigger-time resolution, one resolution function shared across callers
- [Source: ...ARCHITECTURE-SPINE.md#AD-4] — `Notification`/`NotificationTarget`/`NotificationDelivery` data ownership; target spec is relational rows, never JSON
- [Source: ...ARCHITECTURE-SPINE.md#AD-6] — Source System ingestion contract; Team/territory data provenance
- [Source: ...ARCHITECTURE-SPINE.md#AD-9] — opt-in consent enforced at resolution
- [Source: ...ARCHITECTURE-SPINE.md#AD-11] — `ReportSchedule` bound to Story 4.4, not this story; scope-decision basis for Task 5's env-var placeholder choice
- [Source: ...ARCHITECTURE-SPINE.md#Consistency Conventions] — Operational Day = Asia/Dhaka calendar day, ISO 8601 UTC storage, Cr BDT presentation-edge-only formatting
- [Source: ...ARCHITECTURE-SPINE.md#Capability → Architecture Map] — CAP-3 lives in `scheduler/`, `domain/notifications`, `adapters/whatsapp_twilio`
- [Source: _bmad-output/specs/spec-growthtrack/sample-whatsapp-report.md] — exact reference format/figures for AC #2
- [Source: _bmad-output/specs/spec-growthtrack/entities.md] — baseline field inventory (no `Team.territory` field — basis for the territory-mapping ASSUMPTION)
- [Source: _bmad-output/planning-artifacts/prds/prd-GrowthTrack-2026-07-14/prd.md] — Glossary "Territory"/"Operational Day" definitions (lines 79, 89), FR-6, the 07:00 Asia/Dhaka default schedule assumption (lines 176, 337, 357), "condensed to names only" (line 172)
- [Source: _bmad-output/implementation-artifacts/4-1-compose-send-manual-notification.md] — prior story's full implementation: domain services, ports, adapters, migration, Review Findings (dispatch-loop exception handling this story's extraction must preserve)
- Codebase (read directly, current as of this story's creation): `domain/{models,notifications,metrics,ingestion}.py`, `ports/{notifications,users,teams,doctors,audit}.py`, `adapters/persistence/{notifications,advisory_locks,import_runs,users,teams}.py`, `scheduler/main.py`, `config.py`, `.env.example`, `docker/docker-compose.yml`, `scripts/seed_demo_data.py`, `web/src/utils/format.ts`, `api/dashboard/routes.py`, `alembic/versions/{c4a8f21e6b3d,dba27c6b09b6}_*.py`, `tests/conftest.py`

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
