# GrowthTrack — web

React 19 + MUI admin portal, built with Vite.

```
npm install
npm run dev         # dev server
npm run lint         # eslint
npm run typecheck    # tsc -b
npm run test         # vitest
npm run build         # production build (tsc -b && vite build)
```
